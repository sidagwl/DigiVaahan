<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="cf.samarpan.digivaahan">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"></uses-permission>

    <application
        android:name=".app.AppController"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".user.RegisterActivity" />
        <activity android:name=".user.LoginActivity" />
        <activity android:name=".user.ProfileActivity" />
        <activity android:name=".user.EditProfileActivity" />
        <activity android:name=".puc.LoginActivity" />
        <activity android:name=".puc.ProfileActivity" />
        <activity android:name=".puc.EditProfileActivity" />
        <activity android:name=".user.ViewPUCActivity" />
        <activity android:name=".user.ViewInsuranceActivity" />
        <activity android:name=".puc.GeneratePUCActivity" />
        <activity android:name=".puc.ViewPUCActivity" />
        <activity android:name=".puc.MainActivity"></activity>
        <activity android:name=".user.PDFfromServerActivity"></activity>

    </application>

</manifest>